package core;

import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

public class Example {

    //무엇을 풀어야 할까요?
    // String 타입의 이진법을 Int 타입의 십진법으로 바꿔라(재귀함수)

//    private int binaryToDecimal(String str) {
////        int spot = str.charAt(0) - '0';
////        System.out.println("spot :" + spot);
////        int pow = (int) Math.pow(2, spot);
//
//        return ;
//    }

    @Test
    void result() {
//        assertThat(binaryToDecimal("")).isEqualTo(0);
//        assertThat(binaryToDecimal("0")).isEqualTo(1);
//        assertThat(binaryToDecimal("1")).isEqualTo(2);
//        assertThat(binaryToDecimal("11")).isEqualTo(3);
    }
}
