#!/bin/bash
#Generated by JetBrains Toolbox 1.25.12627 at 2022-08-03T09:28:36.504683

declare -a ideargs=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    ideargs+=("\"$o\"")
  else
    ideargs+=("$o")
  fi
done

open -na "/Users/jihwooon/Library/Application Support/JetBrains/Toolbox/apps/datagrip/ch-0/222.3345.126/DataGrip.app/Contents/MacOS/datagrip" $wait --args "${ideargs[@]}"