#!/bin/bash
#Generated by JetBrains Toolbox 1.25.12999 at 2022-09-15T23:39:28.153915

declare -a ideargs=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    ideargs+=("\"$o\"")
  else
    ideargs+=("$o")
  fi
done

open -na "/Users/jihwooon/Library/Application Support/JetBrains/Toolbox/apps/datagrip/ch-0/222.4167.25/DataGrip.app/Contents/MacOS/datagrip" $wait --args "${ideargs[@]}"